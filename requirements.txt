# ==============================================================
# game-translator 依赖
# 安装：pip install -r requirements.txt
# ==============================================================

# --- 必装（核心翻译 + 向量记忆 + Unity 支持）---
openai>=1.0.0           # DeepSeek API（兼容 OpenAI SDK）
requests>=2.28.0        # HTTP 请求
numpy>=1.24.0           # 向量库（翻译一致性，trigram 方案无额外依赖）
UnityPy>=1.20.0         # Unity 资源扫描

# --- 可选：GUI 拖拽 ---
tkinterdnd2>=0.3.0

# --- 可选：Anthropic Claude ---
anthropic>=0.30.0

# --- 可选：Frida 注入（非 Unity 引擎）---
frida-tools>=12.0.0
pycryptodome>=3.20.0

# --- 可选：额外引擎支持 ---
unrpa>=2.0.0            # RenPy 解包
lz4>=4.0.0              # LZ4 压缩
py7zr>=0.20.0           # 7z 解压

# --- 可选：BGE 中文向量模型（需 C++ 编译器）---
# pip install llama-cpp-python
# 模型放: models/bge-small-zh-v1.5.gguf
# 不装则自动用 trigram 向量（效果足够）
